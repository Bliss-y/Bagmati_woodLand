<!-- include header -->
<%- include ('_header') %>
<!-- include header -->

<% if(data._id){ %>
  <% post = "/edituser/students&"+data._id %>

  <% }else{  %>

  <% post = "/adduser/students" %>
  <% data = "" %>

  <% } %>

<div class = "table-main"> 
  <form method="POST" action="/admin<%= post %>">
  

    <div  class="form-group"  id = "Update">
      <input type="tetxt" id="idHide" name = "_id" value="<%= data._id %>" readonly>
      <label for="email">Full Name</label>
      <input type="text" id="email" name = "name" value="<%= data.name %>" required>
    </div>
    <div  class="form-group"  id = "Update">
      <label for="email">Email address</label>
      <input type="email" id="email" name = "email" value="<%= data.email %>" required>
    </div>
    <div  class="form-group"  id = "Update">
      <label for="email">Phone Number</label>
      <input type="text" id="email" name = "phoneNumber" value="<%= data.phoneNumber %>" required>
    </div>
      <div  class="form-group"  id = "Update">
      <label for="email">Address</label>
      <input type="text" id="email" name = "address" value="<%= data.address  %>" required>
    </div>
      <div  class="form-group"  id = "Update">
      <label for="email">Dob</label>
      <input type="date" id="email" name = "dob" value="<%= data.dob  %>" required>
    </div>
     <div class="form-group">
       <label for="email">Courses</label>
      <select name = "course" class="sel-opt" required>
        <option> none </option>
        <% for(let x in courses) {%>
        <option value="<%= courses[x]._id %>"><%= courses[x].name %></option>
        <% }%>
    </select>
     </div>
  
    <a href="#" class="sub-link">
     <button type="submit" class="submit">Submit</button>
    </a>

  </form>
  
</div>



<%- include ('../include/_footer') %>

<!-- header -->