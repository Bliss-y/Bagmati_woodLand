<!--
	Takes :- Id, fullname, Gender, BirthDate, Address, Contact number, Email
-->


<div class="table-main">
	<h1 class = "table-heading">Profile Details</h1>
	<table class="table table-all">
		<% var keys = Object.keys(data)%>
		<% for (let key in keys) { %>
		<tr>
			<th><%= keys[key] %></th>
			<td><%= data[keys[key]]%></td>
		</tr>
	<% } %>
	</table>

</div>