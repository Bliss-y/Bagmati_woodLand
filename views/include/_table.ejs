<!-- <script src="/js/table.js"></script> -->


	
	<div class = "add-std">
		<button>
			
			<a href="/admin/adduser/<%= dataType %>" class="add-button"><i class="fa fa-plus"></i>Add</a>
		</button>
	</div>
	<% if (data.length > 0){%>
	<table id="realTable">
		<% var keys = Object.keys(data[0])%>
		<%  keys.splice(keys.indexOf('_id'),1) %>
		<% for (let key in keys) { %>
		<th><%= keys[key] %></th>
	<% } %>
			<th span="2" colspan="2">actions</th>	
	<% for(i = 0; i < data.length; i++){ %>
		<tr>
			<% for(let c in keys){ %>
			<td><%= data[i][keys[c]]%></td>
			<% } %>
			<td><a href="/admin/edituser/<%= dataType %>&<%= data[i]._id %>" id = "">Edit</a></td>
			<td><a  href="/admin/remove/<%= dataType %>&<%= data[i]._id %>" class="removeData" onclick="return confirm('Confirm to remove');">Remove</a></td>
		</tr>
		
	<% } %>
	
	</table>


<% } %>